<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.digitalhouse.app_cinema">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_cinema">
        <activity
            android:name=".activities.TelaParaAssistir"
            android:exported="false" />
        <activity
            android:name=".activities.TelaFavoritos"
            android:exported="false" />
        <activity
            android:name=".activities.TelaRedefinirSenha"
            android:exported="false" />
        <activity
            android:name=".activities.TelaRecuperarSenha"
            android:exported="false" />
        <activity
            android:name=".activities.TelaCriarConta"
            android:exported="false" />
        <activity
            android:name=".activities.TelaLogin"
            android:exported="false" />
        <activity
            android:name=".activities.TelaInicial"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>